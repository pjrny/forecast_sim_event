# Archived demo
This folder holds the previous single-file dashboard demo.
Its math used ratio/percentage allocation hacks (e.g. BASE_ATTENDEES=4798 style demos).
**Do not use as the Forecaster engine.** v1 on this branch uses Master Budget `budget_model.json` reconciled at N=7888.

